import React from 'react';
import styled from 'styled-components';

import { Div } from 'components/atom/Box';

function OXTitleForm() {
  return <Wrapper></Wrapper>;
}

export default OXTitleForm;

const Wrapper = styled.div`
  display: flex;
`;
